import 'package:flutter/material.dart';

class AllHaircutPage extends StatelessWidget {
  const AllHaircutPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(),
        body: const SingleChildScrollView(
            child: Padding(
          padding: EdgeInsets.all(20),
          child: Center(
            child: Column(),
          ),
        )));
  }
}
